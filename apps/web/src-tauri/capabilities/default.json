{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions granted to the cLAWd shell. We only allow what the shell actually needs: spawn the bundled backend sidecar, read its events, and let the WebView talk to 127.0.0.1:8000 (the FastAPI). Everything else is implicitly denied.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "shell:allow-spawn",
      "allow": [
        {
          "name": "binaries/cLAWd-backend",
          "sidecar": true,
          "args": []
        }
      ]
    },
    "shell:allow-kill"
  ]
}
